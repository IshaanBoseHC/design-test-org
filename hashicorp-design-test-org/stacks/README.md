# Stacks

Stacks are not yet fully automatable via Terraform. Use the UI to create stack
deployments that reference the stack definitions in this folder.

## Suggested stacks

- `stacks/dev.hcl`
- `stacks/staging.hcl`
- `stacks/prod.hcl`
