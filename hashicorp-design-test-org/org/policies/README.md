# Sentinel Policies

This folder is a placeholder for sentinel policies. Policies are created in
`org/main.tf` with inline content for now. For a more realistic setup, move
policy files into this folder and reference them using `file()` in the
`tfe_policy` resource.
